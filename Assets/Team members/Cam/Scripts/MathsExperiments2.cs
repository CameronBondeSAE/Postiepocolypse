using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MathsExperiments2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Vector3.Angle()
		// Vector3.Cross()
		// Vector3.Dot()
		// Vector3.Reflect()
		// Vector3.Project()
		// Vector3.ProjectOnPlane()
		
		// Mathf.PerlinNoise()
		// Mathf.Atan2()
		// Mathf.Sin()
		// Mathf.Cos()
		
		// Quaternion.RotateTowards()
		// Quaternion.FromToRotation()
		// Quaternion.LookRotation()
		
		// Random.InitState();
		// Random.insideUnitSphere; etc
	}

    // Update is called once per frame
    void Update()
    {
        
    }
}
